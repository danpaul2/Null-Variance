import React from "react";
import ReactDOM from "react-dom/client";
import NawiPortalApp from "../frontEnd.JSX";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NawiPortalApp />
  </React.StrictMode>
);
